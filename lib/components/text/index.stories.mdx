import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Text } from "@lib/components/text";

<Meta title="Components/Atom/Text" component={Text} />

# Text

Text is the component that you use, for get user taps and clicks.

```js
import { Text } from "domeui-components";
```

### Classnames
```css
/* Text */
.DUI-text {}
```

<ArgsTable of={Text} />

<Canvas>
  <Story
    name="Paragraph"
    args={{ ...Text.defaultProps, label: "Text default" }}
  >
    {(args) => <Text {...args} />}
  </Story>
</Canvas>
